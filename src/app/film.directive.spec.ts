import { FilmDirective } from './film.directive';

describe('FilmDirective', () => {
  it('should create an instance', () => {
    const directive = new FilmDirective();
    expect(directive).toBeTruthy();
  });
});
